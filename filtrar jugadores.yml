info:
  name: filtrar jugadores
  type: http
  seq: 11

http:
  method: GET
  url: http://localhost:3000/jugadores/filtros/buscar?posicion=Delantero&activo=true
  params:
    - name: posicion
      value: Delantero
      type: query
    - name: activo
      value: "true"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
