info:
  name: estadisticas jugadores
  type: http
  seq: 12

http:
  method: GET
  url: http://localhost:3000/estadisticas/jugadores?campo=goles_temporada
  params:
    - name: campo
      value: goles_temporada
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
