info:
  name: crear gol
  type: http
  seq: 9

http:
  method: POST
  url: http://localhost:3000/goles
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
